CREATE TABLE results (
   id INTEGER PRIMARY KEY AUTOINCREMENT,
   year text,
   month text,
   place num, 
   lname text, 
   fname text,
   uspsa text,
   class text,
   division text, 
   pf text,
   lady text,
   mil text, 
   law text, 
   for text,
   age text,
   points num,
   stgperc num
);
